version: '3.3'
services:
  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'db'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'root'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'root'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      # <Port exposed> : < MySQL Port running inside container>
      - '3306:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
      # Where our data will be persisted
    volumes:
      - my-db:/var/lib/mysql

  Item-Service:
    image: golang:1.11
    build: .
    ports:
        - "8080:8080"
    volumes: 
        - .:/go/src/github.com/Shangye-space/Item-Service

    expose:
      - '8080'
    
    command: go run /go/src/github.com/Shangye-space/Item-Service/main.go
# Names our volume
volumes:
  my-db: